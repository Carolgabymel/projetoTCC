<!DOCTYPE html>
<html lang="pt-br">
  <%- include('components/head') %>
  <body>
    <main>
      <%- include('components/header-main') %>
      <div class="form-admin">
        <form action="/" method="post" class="form form-login">
          <h1>admin</h1>
          <div class="form-group">
            <div class="input">
              <input
                type="text"
                name="admin_email"
                class="admin-login"
                id="admin-login"
                placeholder=""
              />
              <label for="admin-login">Admin User Login</label>
            </div>
            <div class="input">
              <input
                type="password"
                name="admin_password"
                class="admin-password"
                id="admin-password"
                placeholder=""
              />
              <label for="admin-password">Admin Password</label>
            </div>
            <div class="btn-group">
              <div class="btn">
                <button>login</button>
              </div>
            </div>
            <% if (msgError) { %>
            <div class="msgError"><%= msgError %></div>
            <% } %><% if (msgSuccess) { %>
            <div class="msgSuccess"><%= msgSuccess %></div>
            <% } %>
          </div>
        </form>
      </div>
    </main>
  </body>
</html>
